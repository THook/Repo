
#include <stdio.h>

int	main(int ac, char **av)
{

	if (ac != 3)
	{
		printf("unvalid nbr of arguments\n");
		return (1);
	}
	else
	{
	//	kill(ft_atoi(av[1]), sig); kills father's process
	//	si av[1] correspond à un pid valide, éxécuter le programme, sinon retourner une erreur: "pid ne correspond à aucun processus en cours"
	}
	return (0);


}
